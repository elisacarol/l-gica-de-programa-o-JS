
//     indices       0      1       2
const variavel = ['Joao', 'Ana', 'Samuel']   // Array - armazena numeros, strings e combinacoes com números e strings 
                                            // não tem limite para armazenamento de dados. Como ele encontra os valores
                                            // Os valores sao indexados com o 0, 1, 2, 3
// acessa assim a variavel dentro do array:

console.log(variavel [1]);

// o indice = posicao -1

// 

// junto com os vetores aprendemos a forma de "laçar" com o FOR

const variavel = [ 'A', 'B', 'C', 'D', 'E', 'F', 'G'];

for(let contador = 0;contador < 5; contador ++) {// no FOR definir(criar a variavel) de onde ele começa 
  //for(let i = 0; i < 5; i ++)                 //a contar "0", o percursso "CONTADOR < 5" e o e o final "CONTADOR ++".
                                                // O CONTADOR = i (index)

//for(let i = 0; i < variavel.length ; contador ++) // a variavel length percorre até o final                                                  


console.log(variavel[contador]) // quero que imprima para mim a variavel entao chamo a variavel CONTADOR ou i
}

