const a = `a`, b = `A`; 
const condicao = a === b;

if (condicao) {
    console.log(`VERDADEIRO`);
}else{
    console.log(`FALSO`);
    
    //Operadores de comparação
    // == é igual
    // != não é igual
    // > maior que
    // < menor que
    // >= maior ou igual
    // <= menor ou igual
    // === é estritamente igual (valor E tipo)
    // !== não é estrigual (valor OU tipo)

    // true -> palavra reservada para o valor "VERDADEIRO"
    // false -> palavra reservada para o valor "FALSO"
    // ! -> "NÃO"

    // Estrutura IF-ELSE